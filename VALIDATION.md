# Current validation

The current package is **Colosseum Overhaul 1.0 — cache options hotfix**.
Read `VALIDATION_CACHE_OPTIONS_1.0.md` for the current checks and scope.
`VALIDATION_1.0.md` and older reports are retained as historical records.
Current logs are under `validation/cache_options_1_0/`; fresh-extraction runs
and screenshots are included in the separate hotfix evidence archive.
