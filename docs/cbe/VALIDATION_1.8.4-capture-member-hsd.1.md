# Validation — 1.8.4 Capture Member HSD

Static validation target for the retail capture-source extraction rewrite.

- Version: `1.8.4-capture-member-hsd.1`.
- Capture index revision: 6.
- Full snatch member HSD roots are enumerated before Type-2 payload candidates.
- Capture-only deep HSD archive scanning is available beyond the normal 64 KiB fast search.
- Selected member roots compile to static source caches containing source geometry/material/texture data.
- Capture bank structural readiness is independent from 12/12 source completeness; fallback rows are explicit and never represented as source-backed.
- Existing capture presentation changes from 1.8.1 are retained.
