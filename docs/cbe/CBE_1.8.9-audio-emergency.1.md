# CBE 1.8.9-audio-emergency.1

Emergency audio regression rollback while retaining the 1.8.9 MoveFX/video fixes.

- Restores the exact canonical 1.8.8 shared MusyX renderer (`PortableMusyX.lua`).
- Restores the exact canonical 1.8.8 audio extraction/cache transaction (`AudioProbe.lua`).
- Restores the exact canonical 1.8.8 audio cache validation contract (`CacheManager.lua`).
- Restores canonical-v8 identity: `lua-musyx-canonical-v8-shared`, 48 kHz, identical platform semantics.
- A cache generated by the broken interim 1.8.9 renderer identity is rejected and regenerated rather than reused.
- Keeps MoveFX v3 / extractor 22 / Waza 6, GPT1 TXG container-axis texture selection, and Waza source pose lock.
- Keeps visual runtime independent from audio failure and writes canonical source/render diagnostics.
